<TabViewer>:
    bar_width: 10
    scroll_type: ['bars', 'content']


<SongBuilder>:
    TabViewer:
        id: tabviewer
        pos_hint: {'center_y': 0.5}
    EditToolbar:
        tabviewer: tabviewer
        size_hint: [1, 0]

<TabWidget>:
    size_hint: [None, None]
    height: self.step_y * 10
    canvas.before:
        # White background.
        Color:
            rgba: (1, 1, 1, 1)
        Rectangle:
            size: (self.measure_end - self.x, self.height)
            pos: self.pos
        # Selected overlay.
        Color:
            rgba: (1, 1, 0, self.selected_opac)
        Rectangle:
            size: (self.measure_end - self.x, self.height)
            pos: self.pos
        # Add lines.
        Color:
            rgba: (0, 0, 0, 1)
        # Measure start line.
        Line:
            points:
                (self.x + self.measure_start, self.y + self.step_y * 8,
                self.x + self.measure_start, self.y + self.step_y * 3)
        # Staff lines.
        Line:
            points:
                (self.x, self.y + self.step_y * 3,
                self.x + self.measure_end, self.y + self.step_y * 3)
        Line:
            points:
                (self.x, self.y + self.step_y* 4,
                self.x + self.measure_end, self.y + self.step_y * 4)
        Line:
            points:
                (self.x, self.y + self.step_y * 5,
                self.x + self.measure_end, self.y + self.step_y * 5)
        Line:
            points:
                (self.x, self.y + self.step_y * 6,
                self.x + self.measure_end, self.y + self.step_y * 6)
        Line:
            points:
                (self.x, self.y + self.step_y * 7,
                self.x + self.measure_end, self.y + self.step_y * 7)
        Line:
            points:
                (self.x, self.y + self.step_y * 8,
                self.x + self.measure_end, self.y + self.step_y * 8)
        # Measure end line.
        Line:
            points:
                (self.x + self.measure_end, self.y + self.step_y * 8,
                self.x + self.measure_end, self.y + self.step_y * 3)
        # Open repeat lines.
        Color:
            rgba: (0, 0, 0, self.open_repeat_opac)
        Rectangle:
            pos: (self.x + self.measure_start, self.y + self.step_y * 3)
            size: (5, self.step_y * 5)
        Ellipse:
            pos: (self.x + self.measure_start + 10, self.y + self.step_y * 4.25)
            size: (6, 6)
        Ellipse:
            pos: (self.x + self.measure_start + 10, self.y + self.step_y * 6.25)
            size: (6, 6)
        # Close repeat lines.
        Color:
            rgba: (0, 0, 0, self.close_repeat_opac)
        Rectangle:
            pos: (self.measure_end - 5 , self.y + self.step_y * 3)
            size: (5, self.step_y * 5)
        Ellipse:
            pos: (self.measure_end - 16, self.y +  self.step_y * 4.25)
            size: (6, 6)
        Ellipse:
            pos: (self.measure_end - 16, self.y + self.step_y * 6.25)
            size: (6, 6)


<EditToolbar>:
    EditToolbarButton:
        id: copy_btn
        text: "Copy"
        on_press: root.tabviewer.copy()
    EditToolbarButton:
        id: paste_btn
        text: "Paste"
        x: copy_btn.right
    EditToolbarButton:
        id: delete_btn
        text: "Delete"
        x: paste_btn.right
        on_press: root.tabviewer.delete()

    EditToolbarButton:
        id: rebuild_btn
        text: "Rebuild"
        x: delete_btn.right
        on_press: root.tabviewer.rebuild()
<EditToolbarButton@Button>:
    size_hint: [None, None]
    size: [150, 50]


<CopyPopup>:
    size_hint: [None, None]
    size: [200, 60]
    pos_hint: {'center': [0.5, 0.5]}
    separator_color: (0,0,0,0)
    title: 'Measures Copied'
    title_align: 'center'
    font_size: 18